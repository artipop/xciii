{
    "name": "deffun-site",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "build:all": "npm run build && npm --prefix ../docs/guide run build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "vite": "^7.3.6"
    }
}
